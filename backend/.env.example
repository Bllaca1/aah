PORT=3001
NODE_ENV=development

# CORS Configuration
# For development with Vite frontend (default):
CORS_ORIGIN=http://localhost:5173
# For allowing all origins (disables credentials - not recommended for production):
# CORS_ORIGIN=*
# For multiple origins (comma-separated):
# CORS_ORIGIN=http://localhost:5173,http://localhost:3000

# JWT Secrets (CHANGE THESE IN PRODUCTION!)
JWT_ACCESS_SECRET=your-super-secret-access-token-key-change-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-token-key-change-in-production

# Session Secret (CHANGE THIS IN PRODUCTION!)
SESSION_SECRET=your-super-secret-session-key-change-in-production

# Email Configuration (SMTP)
# For development, you can use Ethereal Email: https://ethereal.email/
SMTP_HOST=smtp.ethereal.email
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-ethereal-email@ethereal.email
SMTP_PASSWORD=your-ethereal-password

# Email Sender Info
EMAIL_FROM=noreply@betduel.com
EMAIL_FROM_NAME=BetDuel

# Frontend URL (for email links)
FRONTEND_URL=http://localhost:5173
