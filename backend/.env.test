# Test Environment Variables
NODE_ENV=test
PORT=3001

# Database
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/betduel_test?schema=public"

# JWT
JWT_SECRET=test-jwt-secret-key-for-testing-only-not-for-production
JWT_REFRESH_SECRET=test-jwt-refresh-secret-key-for-testing-only
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Session
SESSION_SECRET=test-session-secret-for-testing-only

# Email (Mock for testing)
EMAIL_HOST=smtp.ethereal.email
EMAIL_PORT=587
EMAIL_USER=test@example.com
EMAIL_PASSWORD=test-password
EMAIL_FROM=noreply@betduel.com

# Frontend URL
FRONTEND_URL=http://localhost:5173

# Rate Limiting (Relaxed for tests)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=1000
